name: 'job_market_dbt'
version: '1.0.0'

profile: 'job_market_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_market_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
